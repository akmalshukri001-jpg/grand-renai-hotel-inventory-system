import 'package:flutter/material.dart';

class StyleConstants {
  // The light turquoise/off-white background of the whole app screen
  static const Color appScreenBg = Color(0xFFE2EBE9);

  // The rich terracotta orange container card holding the form
  static const Color formCardBg = Color(0xFFC05621);

  // Clear white for the text field inputs inside the card
  static const Color inputFieldBg = Colors.white;

  // Transparent white for unselected role options
  static final Color unselectedRoleBg = Colors.white.withOpacity(0.25);
}