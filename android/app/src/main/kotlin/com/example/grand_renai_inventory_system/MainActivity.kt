package com.example.grand_renai_inventory_system

import io.flutter.embedding.android.FlutterActivity

class MainActivity: FlutterActivity() {
    // Modern Flutter handles plugin auto-registration behind the scenes.
    // Leaving this class empty forces Flutter to use its built-in engine mapping.
}